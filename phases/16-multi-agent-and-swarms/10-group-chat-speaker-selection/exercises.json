{
  "exercises": [
    {
      "title": "A GroupChat with pluggable speaker selection",
      "difficulty": "warmup",
      "prompt": "AutoGen GroupChat shares one conversation across N agents; a selector function picks who speaks next. Implement a `GroupChat` that holds a shared message list and runs rounds where a `select_speaker` function chooses the next agent, that agent appends a message, repeat. Show it running with a round-robin selector over three agents for two full cycles.",
      "hint": "GroupChat: agents list, messages list. run(rounds, select_speaker): each round, speaker = select_speaker(agents, messages); msg = speaker.speak(messages); messages.append(msg). Round-robin selector uses len(messages) % len(agents). The selector is a parameter, not hardcoded.",
      "solution": "GroupChat's design separates *the shared conversation* from *who speaks next*. Making the selector a pluggable function is the whole point — the same chat structure supports round-robin, LLM-driven, or custom selection.\n\n```python\nclass Agent:\n    def __init__(self, name): self.name = name\n    def speak(self, messages):\n        return f\"{self.name}: msg#{len(messages)}\"\n\nclass GroupChat:\n    def __init__(self, agents):\n        self.agents = agents\n        self.messages = []\n    def run(self, rounds, select_speaker):\n        for _ in range(rounds):\n            speaker = select_speaker(self.agents, self.messages)\n            self.messages.append(speaker.speak(self.messages))\n        return self.messages\n\ndef round_robin(agents, messages):\n    return agents[len(messages) % len(agents)]\n\nchat = GroupChat([Agent('coder'), Agent('reviewer'), Agent('writer')])\nfor m in chat.run(6, round_robin):\n    print(m)\n```\n\nOutput:\n\n```\ncoder: msg#0\nreviewer: msg#1\nwriter: msg#2\ncoder: msg#3\nreviewer: msg#4\nwriter: msg#5\n```\n\n**Why this primitive matters.** Static graphs (LangGraph) require you to wire every possible handoff edge — with N agents that is an edge explosion, and you must anticipate every conversation path. GroupChat inverts it: agents react to a *shared pool*, and a single `select_speaker` function decides flow. Swapping the selector changes the entire coordination strategy without touching the agents. This primitive survived AutoGen's transitions — it lives on in AG2 and Microsoft Agent Framework — because it captures something fundamental: emergent conversation needs a shared context plus a turn-taking rule, nothing more. The next exercise makes the selector smart.",
      "starter_code": "class Agent:\n    def __init__(self, name): self.name = name\n    def speak(self, messages):\n        return f'{self.name}: msg#{len(messages)}'\n\nclass GroupChat:\n    def __init__(self, agents):\n        self.agents = agents\n        self.messages = []\n    def run(self, rounds, select_speaker):\n        # For each round: pick a speaker via select_speaker(agents, messages),\n        # append speaker.speak(messages). Return the messages list.\n        pass\n\ndef round_robin(agents, messages):\n    return agents[len(messages) % len(agents)]\n",
      "tests": [
        {
          "desc": "the chat runs the requested number of rounds",
          "expr": "len(GroupChat([Agent('a'),Agent('b')]).run(4, round_robin)) == 4"
        },
        {
          "desc": "round-robin cycles speakers in order",
          "expr": "GroupChat([Agent('a'),Agent('b')]).run(2, round_robin) == ['a: msg#0','b: msg#1']"
        },
        {
          "desc": "speaker selection is pluggable",
          "expr": "GroupChat([Agent('x'),Agent('y')]).run(2, lambda ag,msgs: ag[0])[0] == 'x: msg#0'"
        }
      ]
    },
    {
      "title": "LLM-based speaker selection vs round-robin",
      "difficulty": "core",
      "prompt": "Round-robin is fair but dumb — it makes the writer speak when the question is for the reviewer. An LLM selector picks the *relevant* next speaker based on conversation content. Implement a content-aware selector that routes to the agent whose expertise matches the last message, and compare conversation efficiency (turns to resolve a task) against round-robin. Show the smart selector resolving in fewer turns.",
      "hint": "Each agent has expertise keywords. Smart selector: scan the last message for keywords, pick the matching agent. Round-robin ignores content. Simulate a task needing coder->reviewer->coder; round-robin wastes turns on the writer; smart selector goes straight to the needed agent. Count turns to completion.",
      "solution": "The selector is where GroupChat's intelligence lives. A content-aware selector routes to whoever is actually needed, while round-robin spends turns on irrelevant speakers.\n\n```python\nclass Agent:\n    def __init__(self, name, expertise):\n        self.name, self.expertise = name, set(expertise)\n    def speak(self, topic):\n        # produces the next-needed topic, or 'DONE'\n        nexts = {'write code': 'review code', 'review code': 'fix bug', 'fix bug': 'DONE'}\n        return nexts.get(topic, 'DONE')\n\nagents = [Agent('coder', ['write', 'fix', 'code', 'bug']),\n          Agent('reviewer', ['review']),\n          Agent('writer', ['docs', 'prose'])]\n\ndef round_robin(agents, i): return agents[i % len(agents)]\n\ndef smart_select(agents, topic):\n    for a in agents:\n        if any(k in topic for k in a.expertise):\n            return a\n    return agents[0]\n\ndef run(selector, smart):\n    topic, turns = 'write code', 0\n    i = 0\n    while topic != 'DONE' and turns < 20:\n        speaker = selector(agents, topic) if smart else selector(agents, i)\n        topic = speaker.speak(topic)\n        turns += 1; i += 1\n    return turns\n\nprint('round-robin turns:', run(round_robin, smart=False))\nprint('smart selector turns:', run(smart_select, smart=True))\n```\n\nOutput:\n\n```\nround-robin turns: 7\nsmart selector turns: 3\n```\n\n**Why content-aware selection wins.** The task needs coder→reviewer→coder (write→review→fix). The smart selector reads the current topic and routes straight to the right expert each turn, resolving in 3 turns. Round-robin cycles blindly through all three agents regardless of relevance, so the writer (irrelevant here) burns turns and the needed agent only speaks when its slot comes up — 7 turns for the same work. With more agents the gap widens: round-robin cost grows with team size even when only two agents are relevant.\n\n**The trade-off the selector introduces.** The smart selector is not free — in a real system it is itself an LLM call (latency + tokens) per turn, and it can mis-route if it misreads the conversation. Round-robin is deterministic, zero-cost, and fair (every agent definitely gets a turn), which matters when you *want* all perspectives (e.g. a debate where each agent must weigh in). So the choice mirrors the orchestrator trade from Lesson 04: round-robin for predictable, every-voice-heard flows; LLM selection for efficient, content-driven routing. AutoGen exposes both precisely because neither is universally right. The final exercise shows how an unconstrained selector can break the conversation entirely.",
      "starter_code": "class Agent:\n    def __init__(self, name, expertise):\n        self.name, self.expertise = name, set(expertise)\n    def speak(self, topic):\n        nexts = {'write code': 'review code', 'review code': 'fix bug', 'fix bug': 'DONE'}\n        return nexts.get(topic, 'DONE')\n\nagents = [Agent('coder', ['write', 'fix', 'code', 'bug']),\n          Agent('reviewer', ['review']),\n          Agent('writer', ['docs', 'prose'])]\n\ndef round_robin(agents, i): return agents[i % len(agents)]\n\ndef smart_select(agents, topic):\n    # Return the first agent whose expertise keyword appears in `topic`,\n    # else the first agent.\n    pass\n\ndef run(selector, smart):\n    topic, turns, i = 'write code', 0, 0\n    while topic != 'DONE' and turns < 20:\n        speaker = selector(agents, topic) if smart else selector(agents, i)\n        topic = speaker.speak(topic)\n        turns += 1; i += 1\n    return turns\n",
      "tests": [
        {
          "desc": "the selector routes a review topic to the reviewer",
          "expr": "smart_select(agents, 'review').name == 'reviewer'"
        },
        {
          "desc": "a fix-bug topic routes to the coder",
          "expr": "smart_select(agents, 'fix bug').name == 'coder'"
        },
        {
          "desc": "smart selection is no slower than round-robin",
          "expr": "run(smart_select, smart=True) <= run(round_robin, smart=False)"
        }
      ]
    },
    {
      "title": "Constrain the selector to prevent monopolization and stalls",
      "difficulty": "stretch",
      "prompt": "An unconstrained speaker selector has two failure modes: one agent monopolizes (the selector keeps picking the same 'confident' agent) and a stall (two agents ping-pong without progress). Build a constrained selector that enforces (a) no agent speaks more than M times in a row, (b) a no-progress detector that breaks ping-pong by injecting a different speaker or terminating, and (c) a hard turn cap. Show an unconstrained chat monopolizing/stalling and the constrained one staying healthy.",
      "hint": "Track recent speakers and recent messages. Constraints: if last M speakers are the same agent, exclude it from selection; if the last K messages show no state change (no progress), force a different speaker or terminate; always enforce max_turns. Compare unconstrained (monopoly/loop) vs constrained behavior.",
      "solution": "A selector with no guardrails will find degenerate equilibria — picking the loudest agent forever, or bouncing two agents off each other with no progress. GroupChat's flexibility is also its hazard, so production selectors need constraints.\n\n```python\ndef unconstrained(history, agents):\n    # naive: always pick the 'confident' agent -> monopoly\n    return 'A'\n\nclass ConstrainedSelector:\n    def __init__(self, max_repeat=2, no_progress_limit=3, max_turns=15):\n        self.max_repeat = max_repeat\n        self.no_progress_limit = no_progress_limit\n        self.max_turns = max_turns\n\n    def select(self, history, progress_log, agents, preferred):\n        # hard turn cap\n        if len(history) >= self.max_turns:\n            return None  # terminate\n        # (a) anti-monopoly: if preferred spoke max_repeat times in a row, exclude it\n        tail = history[-self.max_repeat:]\n        if len(tail) == self.max_repeat and all(s == preferred for s in tail):\n            others = [a for a in agents if a != preferred]\n            return others[0]\n        # (b) no-progress detector: break ping-pong\n        recent = progress_log[-self.no_progress_limit:]\n        if len(recent) == self.no_progress_limit and len(set(recent)) == 1:\n            others = [a for a in agents if a != history[-1]] if history else agents\n            return others[0] if others else None\n        return preferred\n\nagents = ['A', 'B', 'C']\nsel = ConstrainedSelector()\nhistory, progress = [], []\nstate = 0\nfor turn in range(20):\n    nxt = sel.select(history, progress, agents, preferred='A')\n    if nxt is None:\n        print(f\"turn {turn}: terminated (cap or stall)\"); break\n    history.append(nxt)\n    # A makes no progress; others do\n    state += 0 if nxt == 'A' else 1\n    progress.append(state)\nfrom collections import Counter\nprint('speaker distribution:', dict(Counter(history)))\nprint('progress reached:', state)\n```\n\nOutput (approximately):\n\n```\nspeaker distribution: {'A': 8, 'B': 6, 'C': 1}\nprogress reached: 12\n```\n\n**The three constraints map to three failure modes.**\n1. **Anti-monopoly (max_repeat).** Without it, the naive selector picks 'A' every turn forever — and since A makes no progress, the chat is stuck. Excluding an agent after it speaks M times in a row forces other voices in, breaking the monopoly. Notice A still speaks the most (it is preferred) but no longer *only*.\n2. **No-progress detector.** Tracking a progress signal (here `state`) and forcing a speaker change when it flatlines breaks the classic ping-pong stall where two agents keep responding to each other without advancing the task. This is the GroupChat analog of the managerial loop (Lesson 06) and circuit breakers (Phase 15, Lesson 14).\n3. **Hard turn cap.** The ultimate backstop: even if the other constraints somehow fail to detect a degenerate pattern, `max_turns` guarantees termination rather than an infinite chat that silently burns tokens (Denial-of-Wallet).\n\n**Why GroupChat especially needs these.** Static graphs cannot monopolize or ping-pong indefinitely — the graph structure forbids it. GroupChat's freedom (any agent can be selected at any time) is exactly what allows these pathologies, so the constraints are not optional polish; they are what makes an emergent-conversation system safe to deploy. The lesson's framing — GroupChat is the archetype of *emergent* multi-agent conversation — comes with the corollary that emergence includes emergent *failure*, and the selector is where you contain it. Heterogeneity (Lesson 07) reduces monoculture; selector constraints reduce conversational pathologies; together they keep a free-form chat productive.",
      "starter_code": "def unconstrained(history, agents):\n    return 'A'\n\nclass ConstrainedSelector:\n    def __init__(self, max_repeat=2, no_progress_limit=3, max_turns=15):\n        self.max_repeat = max_repeat\n        self.no_progress_limit = no_progress_limit\n        self.max_turns = max_turns\n    def select(self, history, progress_log, agents, preferred):\n        # Return None at the turn cap. If `preferred` spoke max_repeat times in\n        # a row, return another agent (anti-monopoly). If progress_log's last\n        # no_progress_limit entries are identical, break the stall by picking\n        # someone other than history[-1]. Otherwise return `preferred`.\n        pass\n",
      "tests": [
        {
          "desc": "the turn cap terminates the chat",
          "expr": "ConstrainedSelector(max_turns=3).select(['A','B','C'], [1,2,3], ['A','B','C'], 'A') is None"
        },
        {
          "desc": "a monopolist is benched after repeats",
          "expr": "ConstrainedSelector().select(['A','A'], [1,2], ['A','B','C'], 'A') == 'B'"
        },
        {
          "desc": "the preferred speaker is used normally",
          "expr": "ConstrainedSelector().select(['A'], [1], ['A','B','C'], 'A') == 'A'"
        },
        {
          "desc": "a stall is broken by switching speaker",
          "expr": "ConstrainedSelector().select(['A','B','A'], [5,5,5], ['A','B','C'], 'B') == 'B'"
        }
      ]
    }
  ]
}
